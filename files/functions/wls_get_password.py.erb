##
# This function gets the password at the specified key and decrypts it. The specfied key must point
# to a key in the tree that contains the encrypted password.
#
key                       = '<%= value %>'
encrypted_password        = get(key)
encrypted_password_string = password_to_string(encrypted_password)
value                     = decrypt_value(encrypted_password_string) 
