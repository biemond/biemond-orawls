

m = ls('/FileStores',returnMap='true')

f = open_file("name;directory;target;targettype;domain", tmp_script)
for token in m:
    print '___'+token+'___'
    cd('/FileStores/'+token)

    directory = get_attribute_value('Directory')

    target, targetType = retrieve_target_list('/FileStores/'+token)

    add_index_entry(f, [domain+'/'+token,directory,','.join(target),','.join(targetType),domain])
f.close()
report_back_success()

