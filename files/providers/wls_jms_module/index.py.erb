

m = ls('/JMSSystemResources',returnMap='true')

f = open_file("name;target;targettype;domain", tmp_script)
for token in m:
    print '___'+token+'___'
    cd('/JMSSystemResources/'+token)

    target, targetType = retrieve_target_list('/JMSSystemResources/'+token)

    add_index_entry(f, [domain+'/'+token,','.join(target),','.join(targetType),domain])
f.close()
report_back_success()
