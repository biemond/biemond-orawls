
m = ls('/JMSSystemResources',returnMap='true')

f = open_file("name;jmsmodule;policy;logformat;errordestination;domain", tmp_script)
for token in m:
    print '_jmsmodule_'+token+'___'
    cd('/JMSSystemResources/'+token+'/JMSResource/'+token)

    n = ls('SAFErrorHandlings',returnMap='true')
    for token2 in n:
        cd('/JMSSystemResources/'+token+'/JMSResource/'+token+'/SAFErrorHandlings/'+token2)
        add_index_entry(f, [domain+'/'+token+":"+token2,token,cmo.getPolicy(),cmo.getLogFormat(),cmo.getSAFErrorDestination(),domain])

f.close()
report_back_success()
