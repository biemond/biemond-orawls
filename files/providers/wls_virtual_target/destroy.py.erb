# check the domain else we need to skip this (done in wls_access.rb)
real_domain='<%= domain %>'

name = '<%= virtual_target_name %>'

edit()
startEdit()

try:

    cd('/')
    cmo.destroyVirtualTarget(getMBean('/VirtualTargets/' + name))
    save()
    activate()
    report_back_success()

except:
    report_back_error()
